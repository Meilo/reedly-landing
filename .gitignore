dist/
node_modules/
.env
.env.local
.env.*.local
.DS_Store
.vercel
.superpowers/
.gstack/

# Astro's generated types and content cache. Regenerated by `astro sync`, which
# dev and build run on their own, so committing it only produced churn (and a
# stale schema for a `blog` collection that no longer exists).
.astro/

# Tool scratch output
.playwright-mcp/
